# Local (Vite proxies /api → Flask when this is unset)
# VITE_API_URL=http://127.0.0.1:5555

# Production (Vercel) — set in the Vercel dashboard, not in git:
# VITE_API_URL=https://subscription-tracker-api.onrender.com
